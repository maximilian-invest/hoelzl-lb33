# allround.immo Konfiguration
# Kopiere diese Datei zu .env.local und passe die Werte an

# JWT Secret für Authentifizierung
# Generiere einen sicheren Secret mit: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Hinweis: Alle Immobilien-Konfigurationen werden als JSON in Variablen gespeichert
# Keine Datenbank-Konfiguration erforderlich